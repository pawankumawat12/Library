export interface Book {
  id: string;
  title: string;
  author: string;
  stock: number;
  issued: number;
}

export interface Student {
  id: string;
  name: string;
  email: string;
  seatNumber: string;
  checkInTime: string;
}
